Plugins to install
    1. GIT Plugin 
    2. httpRequest Plugin
    3. mail Pligin

Plugin to configure:
    1. Mail plugin to be configured with smpt server, port and authentication.


Note:

Please add the script in the pipeline and run once to reflect the parameters in the job. The initail run would fail, since the params are not yet configured.